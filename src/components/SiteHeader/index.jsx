import React from "react"
import logo from "./logo.svg"

const SiteHeader = () =>
    <header>
        <div>
            <img src={logo} alt="FutureGov Knowledge"/>
        </div>
    </header>

export default SiteHeader